extends Node2D

var selectedTile:TileSetAtlasSource
@export var tileSet:TileMap

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _input(event):
	if event == KEY_1:
		pass
	if event == KEY_2:
		pass
	if event == KEY_3:
		pass
	if event == KEY_4:
		pass
